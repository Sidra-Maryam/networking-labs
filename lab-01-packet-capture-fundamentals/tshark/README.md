# tshark Exports

This directory contains text-based exports generated from the original `.pcapng` files using `tshark`.

These files provide a command-line representation of the captured traffic, enabling protocol analysis without the Wireshark graphical interface and supporting reproducible examination of the collected evidence.
